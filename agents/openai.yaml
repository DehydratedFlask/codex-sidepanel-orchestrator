interface:
  display_name: "Codex Side-Panel Orchestrator"
  short_description: "Plan here; delegate only to a live OpenCode panel"
  default_prompt: "Use $codex-sidepanel-orchestrator to plan this coding task and delegate implementation only if my OpenCode side-panel session is live."
policy:
  allow_implicit_invocation: true
